<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.gamerz.member.mapper.MemberMapper">
	<select id="memberSelectList" resultType="co.yedam.gamerz.member.service.MemberVO">
		SELECT * FROM MEMBER
	</select>
	
	<select id="memberSelect" resultType="co.yedam.gamerz.member.service.MemberVO">
		SELELCT * FROM MEMBER WHERE MEMBER_ID = #{memberId}
		<if test="memberPassword != null">
			AND MEMBER_PASSWORD = #{memberPassword}
		</if>
	</select>
	
	<insert id="memberInsert" parameterType="co.yedam.gamerz.member.service.MemberVO">
		INSERT INTO MEMBER(member_id, member_password, member_name, member_birth, member_phone, member_address, member_email)
		VALUES(#{memberId}, #{memberPassword}, #{memberName}, #{memberBirth}, #{memberPhone}, #{memberAddress}, #{memberEmail})
	</insert>
	
	<update id="memberUpdate" parameterType="co.yedam.gamerz.member.service.MemberVO">
		UPDATE MEMBER
		<set>
			<if test="memberPassword != null">member_password = #{memberPassword}</if>
			<if test="memberName != null">member_name = #{memberName}</if>
			<if test="memberBirth != null">member_birth = #{memberBirth}</if>
			<if test="memberPhone != null">member_phone = #{memberPhone}</if>
			<if test="memberAddress != null">member_address = #{memberAddress}</if>
			<if test="memberEdate != null">member_edate = #{memberEdate}</if>
			<if test="memberAuthor != null">member_author = #{memberAuthor}</if>
		</set>
		WHERE MEMBER_ID = #{memberId}
	</update>
	
	<delete id="memberDelete" parameterType="co.yedam.gamerz.member.service.MemberVO">
		DELETE MEMBER WHERE MEMBER_ID = #{memberId}
	</delete>
</mapper>